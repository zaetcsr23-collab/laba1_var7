<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#FFF5F5">

    <!-- Первый заголовок -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Сгенерированный список:"
        android:textSize="16sp"
        android:textColor="#8B4B63"
        android:layout_marginBottom="16dp" />

    <!-- Первый прямоугольник -->
    <TextView
        android:id="@+id/generatedListTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFE8E8"
        android:padding="12dp"
        android:text="Здесь будут числа..."
        android:textColor="#8B4B63"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <!-- Кнопка -->
    <Button
        android:id="@+id/executeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Выполнить"
        android:backgroundTint="#FFB6C1"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="16dp" />

    <!-- Второй заголовок -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Результат работы:"
        android:textSize="16sp"
        android:textColor="#8B4B63"
        android:layout_marginBottom="16dp" />

    <!-- Второй прямоугольник -->
    <TextView
        android:id="@+id/resultTextView"
        android:layout_width="match_parent"
        android:layout_height="499dp"
        android:background="#FFE8E8"
        android:gravity="start|top"
        android:padding="12dp"
        android:text="Нажмите кнопку для выполнения"
        android:textColor="#8B4B63"
        android:textSize="14sp" />

</LinearLayout>